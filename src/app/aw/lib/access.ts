/**
 * Access code for the /aw wallet on the public production domain.
 * NOTE: this is obscurity (client-side), not real security — it keeps casual
 * visitors out of the unfinished wallet, not a determined reader of the JS.
 * Change the code here. Direct link that skips the prompt: /aw?key=<code>
 */
export const AW_ACCESS_CODE = 'trilium-forge'
